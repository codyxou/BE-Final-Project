spring:
  datasource:
    username: team
    password: team
    url: jdbc:mysql://localhost:3306/team
    
  jpa:
     hibernate:
       ddl-auto: update
     show-sql: true
     defer-datasource-initialization: true
     
  sql:
    init:
     mode: always
